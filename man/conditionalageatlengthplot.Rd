% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditionalageatlengthplot.R
\name{conditionalageatlengthplot}
\alias{conditionalageatlengthplot}
\title{Conditional age-at-length plot}
\usage{
conditionalageatlengthplot(
  data,
  show_fits = TRUE,
  xlab = "Age (years)",
  ylab = "Length (cm)",
  legend_position = "top",
  size_breaks = c(0.01, 0.25, 0.5),
  size_range = c(0.01, 6),
  fleet = 1,
  ylim = c(NA, NA),
  xlim = c(NA, NA),
  text_size = 12,
  ncol = 3,
  colours = fq_palette("alisecolours")[c(2, 10)]
)
}
\arguments{
\item{data}{Output from ageplot_prep(). A data frame with year (int), bin (int), fleet (int), lbin_low (int), pearson (num), obs (num), sex (int), scenario (int)}

\item{show_fits}{Set to TRUE to show model fits.}

\item{xlab}{Label for x-axis (character). Default is "Year".}

\item{ylab}{Label for y-axis (character). Default is "Spawning biomass (relative)".}

\item{legend_position}{Position of the legend ("none", "left", "right", "bottom", "top", or two-element numeric vector for x and y position). Default is "top".}

\item{size_breaks}{Breaks of size scale for bubbles}

\item{size_range}{Range of size scale for bubbles}

\item{fleet}{Specify which fleet to plot (numeric). By default, fleet 1 will be shown.}

\item{ylim}{A vector of lower and upper y-axis limits (e.g. c(0,1)) (numeric).}

\item{xlim}{A vector of lower and upper x-axis limits (e.g. c(1950, 2020)) (numeric).}

\item{text_size}{Text size (num). Default is 12.}

\item{ncol}{Number of columns for facet_wrap(). Default is 3.}

\item{colours}{A vector of colours used (character).}
}
\value{
Conditional age-at-length plot
}
\description{
Conditional age-at-length plot
}
\examples{
data <- conditionalageatlengthplot_prep_SS(ss_mle,sex_code=1)
conditionalageatlengthplot(data, show_fits=FALSE)
conditionalageatlengthplot(data)
}
